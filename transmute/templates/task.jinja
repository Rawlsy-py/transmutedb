import polars as pl
from typing import TypedDict, List

class {{ object_name|capitalize }}Record(TypedDict):
{%- for field in field_defs %}
    {{ field.name }}: {{ field.type }}
{%- endfor %}

def transform_{{ object_name }}(
    records: List[{{ object_name|capitalize }}Record]
) -> pl.DataFrame:
    """
    Transform a list of {{ object_name|capitalize }} records into a Polars DataFrame.
    Args:
        records: List of {{ object_name|capitalize }}Record dicts.
    Returns:
        pl.DataFrame
    """
    df = pl.DataFrame(records)
    return df

